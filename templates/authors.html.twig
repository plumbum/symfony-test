{% extends 'base.html.twig' %}

{% block body %}
    <h1>Список авторов</h1>
    <h2>Добавить автора</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
